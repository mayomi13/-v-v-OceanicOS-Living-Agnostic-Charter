# Frontend

This folder will contain the web UI (React + TypeScript + Vite).

Next steps:
- Scaffold with `npm init vite@latest` or `create-react-app`
- Add pages: Charter, Proposals, Governance
- Add CI rules and deployment config
