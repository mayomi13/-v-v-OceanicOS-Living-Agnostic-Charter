# Infra

Infrastructure and deployment manifests (Docker, Kubernetes, Terraform, Helm charts) will live here.

Next steps:
- Add `docker-compose` for local dev
- Add deployment manifests for target platform
