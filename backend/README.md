# Backend

This folder will house the API service. Suggested stack: TypeScript + Fastify/Express, PostgreSQL, Prisma.

Next steps:
- Initialize `package.json` and TypeScript config
- Add basic health-check and charter API endpoints
- Add Dockerfile and local dev scripts
